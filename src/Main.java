public class Main {
    public static void main(String[] args) {

        //Создание объекта Child без параметров
        Child child = new Child();

        //Создание объекта Child с параметрами
        Child Ivan = new Child("Ivan");
    }
}